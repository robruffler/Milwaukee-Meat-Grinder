<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
  xmlns:beans="http://www.springframework.org/schema/beans" 
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://www.springframework.org/schema/beans
                         http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                        http://www.springframework.org/schema/security
                         http://www.springframework.org/schema/security/spring-security-3.0.4.xsd">

  <!-- path-type switches intercept urls to check pattern as regex -->                         
	<http auto-config="true" access-denied-page="/" path-type="regex">
  <!-- 
		<intercept-url pattern="/secured/profile**" access="ROLE_SUPERVISOR,ROLE_USER,ROLE_TELLER"/>
		<intercept-url pattern="/faces/protected**" access="ROLE_SUPERVISOR,ROLE_USER,ROLE_TELLER"/>
  -->
    <intercept-url pattern="/user/profile.*" access="ROLE_USER" />		
		<intercept-url pattern="/.*" access="IS_AUTHENTICATED_ANONYMOUSLY" />
		<form-login />
		<!-- look into putting a different value into the remember-me key -->
		<remember-me key="myAppKey"/>
	</http>

	<authentication-manager>
		<authentication-provider user-service-ref="userService">
		  <password-encoder hash="sha"/>
		</authentication-provider>
  </authentication-manager>
  
</beans:beans>